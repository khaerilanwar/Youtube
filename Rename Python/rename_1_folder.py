# Rename File dengan 1 Direktori yang sama
import os

daftar_file = os.listdir()

# for i in range(len(daftar_file)):
#     file = daftar_file[i]
#     nama_baru = file.replace('y2mate.com - ', '')
#     os.replace(file, nama_baru)
print(daftar_file)